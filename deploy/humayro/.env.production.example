# Host-managed runtime variables for the frontend container.
# Keep this file on the server as `.env.production`.

# Optional: override the internal listen port if the container command changes.
# PORT=3000

# Development uses a separate host-managed `.env.development` file. Never copy
# production OAuth or other server-only secrets into that file.
